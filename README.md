# 🤖 Telegram-бот для проверки лабораторных работ

**Выполнила:** Косаева Виктория  
**Группа:** 3393  
**Дата:** 10.10.2025  

---

## 📘 Описание проекта
Этот Telegram-бот предназначен для автоматической проверки лабораторных работ студентов с помощью бесплатных языковых моделей (LLM), доступных через платформу [OpenRouter.ai](https://openrouter.ai).  
Бот позволяет:
- Отправлять файлы **PDF**, **DOCX** и **TXT** для анализа;
- Выбирать одну из нескольких моделей ИИ;
- Сохранять выбранную модель между перезапусками;
- Получать детализированный анализ и оценку по критериям.

---

## ⚙️ Требования
- Python **3.10+**
- Установленные зависимости из `requirements.txt`
- Аккаунт на [OpenRouter.ai](https://openrouter.ai)
- Созданный Telegram-бот через [@BotFather](https://t.me/BotFather)

---

## 📦 Установка и запуск

### 1️⃣ Клонировать или распаковать проект
```bash
cd путь/к/папке
```

### 2️⃣ Создать файл `.env`
```bash
TELEGRAM_BOT_TOKEN=твой_токен_бота
OPENROUTER_API_KEY=твой_API_KEY
```

> ⚠️ Никогда не добавляй `.env` в GitHub!  
> Файл уже добавлен в `.gitignore`.

### 3️⃣ Создать виртуальное окружение
#### Windows:
```bash
python -m venv venv
venv\Scripts\activate
```

#### Linux / macOS:
```bash
python3 -m venv venv
source venv/bin/activate
```

### 4️⃣ Установить зависимости
```bash
pip install -r requirements.txt
```

### 5️⃣ Запустить бота
```bash
python bot.py
```

---

## 🧠 Выбор модели
При запуске в Telegram доступны кнопки:
- **DevStral** (`mistralai/devstral-small-2505:free`) — быстрая и сбалансированная  
- **Mistral** (`mistralai/mistral-7b-instruct:free`) — логичные, формальные ответы  
- **Gemma** (`google/gemma-7b-it:free`) — более креативная  
- **Nous Hermes** (`nousresearch/nous-hermes-2-mistral:free`) — строгий стиль  

Выбранная модель сохраняется в файле `models.json` и восстанавливается при следующем запуске.

---

## 🧾 Тестирование
Результаты тестов приведены в файле [`test_results.txt`](./test_results.txt).  
Для тестирования использовались три модели с разными параметрами `temperature`, `top_p`, `frequency_penalty` и `presence_penalty`.

---

## 📁 Структура проекта
```
📦 laba2_
 ┣ 📜 bot.py
 ┣ 📜 file_utils.py
 ┣ 📜 requirements.txt
 ┣ 📜 README.md
 ┣ 📜 test_results.txt
 ┣ ⚙️ models.json
 ┗ ⚙️ .gitignore
```

---

## ✅ Пример работы
1. Запусти бота и отправь файл (PDF, DOCX или TXT).  
2. Дождись анализа (~1 минута).  
3. Получи отчёт с баллами и рекомендациями.  
4. Можно сменить модель и повторить проверку.


